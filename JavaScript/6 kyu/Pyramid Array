function pyramid(n) {
  let result = [];
  for (let i = 0; i < n; i++) {
    result[i] = [];
    result[i][i] = 1;
  }

  for (let i = 0; i < result.length; i++) {
    result[i].fill(1);
  }

  return result;
}
